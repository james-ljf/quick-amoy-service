server:
  port: 8100
spring:
  main:
    allow-bean-definition-overriding: true

dubbo:
  scan:
    base-packages: com.game.user   #指定扫描基准包
  protocols:
    dubbo:
      name: dubbo   # 协议名称
      port: -1      # 协议端口（ -1 表示自增端口，从 20880 开始）