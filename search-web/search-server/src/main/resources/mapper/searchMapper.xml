<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kuaipin.search.server.repository.impl.mapper.SearchMapper">

    <select id="selectSearchRecordByPage" resultType="com.kuaipin.search.api.entity.SearchRecordDTO">
        SELECT id, search_id, search_keyword, search_extend, u_id, create_time, update_time
        FROM search_record AS sr
        LEFT JOIN user_info AS u
        ON sr.u_id = u.u_id
    </select>

</mapper>